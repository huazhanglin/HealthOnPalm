# 健康智能体 App 技术架构设计

**时间**: 2026-07-08 16:50 GMT+8
**项目名称**: 健康智能体 (Health Agent)
**目标**: 打造一款整合健康数据、提供智能健康建议的移动应用

---

## 一、整体架构设计

### 1.1 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    用户层                                │
├─────────────────────────────────────────────────────────┤
│  Flutter App (移动端)                                   │
│  ├── 对话界面                                           │
│  ├── 健康数据看板                                       │
│  ├── 训练计划管理                                       │
│  └── 个人中心                                           │
└─────────────────────────────────────────────────────────┘
                        ↕ HTTPS / WebSocket
┌─────────────────────────────────────────────────────────┐
│                    API 网关层                            │
├─────────────────────────────────────────────────────────┤
│  FastAPI 后端服务                                       │
│  ├── 用户认证                                           │
│  ├── 对话路由                                           │
│  ├── 健康数据管理                                       │
│  └── 计划生成                                           │
└─────────────────────────────────────────────────────────┘
                        ↕
┌─────────────────────────────────────────────────────────┐
│                    数据层                                │
├─────────────────────────────────────────────────────────┤
│  PostgreSQL (主数据库)                                  │
│  ├── 用户数据                                           │
│  ├── 对话历史                                           │
│  ├── 健康数据                                           │
│  └── 训练计划                                           │
│                                                         │
│  Redis (缓存层)                                         │
│  ├── 会话缓存                                           │
│  ├── 热点数据                                           │
│  └── 限流计数                                           │
└─────────────────────────────────────────────────────────┘
                        ↕
┌─────────────────────────────────────────────────────────┐
│                    AI 服务层                             │
├─────────────────────────────────────────────────────────┤
│  大模型 API                                             │
│  ├── Claude API (主力)                                  │
│  ├── GPT-4 API (备用)                                   │
│  └── DeepSeek API (降级)                                │
│                                                         │
│  本地小模型 (可选)                                       │
│  ├── Gemma 2B / Qwen 1.5B                               │
│  └── 处理简单问答，降低成本                             │
└─────────────────────────────────────────────────────────┘
```

---

## 二、前端架构 (Flutter)

### 2.1 项目结构

```
health_agent_app/
├── lib/
│   ├── main.dart                    # 应用入口
│   ├── app.dart                     # App 配置
│   │
│   ├── core/                        # 核心模块
│   │   ├── config/
│   │   │   ├── app_config.dart      # 应用配置
│   │   │   └── api_config.dart      # API 配置
│   │   ├── constants/
│   │   │   ├── app_colors.dart      # 颜色常量
│   │   │   ├── app_strings.dart     # 字符串常量
│   │   │   └── app_assets.dart      # 资源路径
│   │   ├── theme/
│   │   │   └── app_theme.dart       # 主题配置
│   │   └── utils/
│   │       ├── date_utils.dart      # 日期工具
│   │       ├── validators.dart      # 表单验证
│   │       └── logger.dart          # 日志工具
│   │
│   ├── data/                        # 数据层
│   │   ├── models/
│   │   │   ├── user.dart            # 用户模型
│   │   │   ├── message.dart         # 消息模型
│   │   │   ├── health_data.dart     # 健康数据模型
│   │   │   └── plan.dart            # 训练计划模型
│   │   ├── repositories/
│   │   │   ├── user_repository.dart
│   │   │   ├── chat_repository.dart
│   │   │   └── health_repository.dart
│   │   └── datasources/
│   │       ├── remote/
│   │       │   ├── api_client.dart  # API 客户端
│   │       │   └── api_endpoints.dart
│   │       └── local/
│   │           ├── local_storage.dart
│   │           └── cache_manager.dart
│   │
│   ├── domain/                      # 业务逻辑层
│   │   ├── entities/
│   │   │   ├── user_entity.dart
│   │   │   └── health_entity.dart
│   │   └── usecases/
│   │       ├── chat_usecase.dart
│   │       ├── health_usecase.dart
│   │       └── plan_usecase.dart
│   │
│   ├── presentation/                # 展示层
│   │   ├── screens/
│   │   │   ├── home/
│   │   │   │   ├── home_screen.dart
│   │   │   │   └── home_controller.dart
│   │   │   ├── chat/
│   │   │   │   ├── chat_screen.dart
│   │   │   │   ├── chat_controller.dart
│   │   │   │   └── widgets/
│   │   │   │       ├── message_bubble.dart
│   │   │   │       └── chat_input.dart
│   │   │   ├── health/
│   │   │   │   ├── health_screen.dart
│   │   │   │   └── widgets/
│   │   │   │       ├── health_card.dart
│   │   │   │       └── steps_chart.dart
│   │   │   ├── plan/
│   │   │   │   ├── plan_screen.dart
│   │   │   │   └── widgets/
│   │   │   │       └── plan_card.dart
│   │   │   └── profile/
│   │   │       ├── profile_screen.dart
│   │   │       └── settings_screen.dart
│   │   ├── widgets/                 # 通用组件
│   │   │   ├── common_button.dart
│   │   │   ├── common_card.dart
│   │   │   └── loading_indicator.dart
│   │   └── providers/               # 状态管理
│   │       ├── user_provider.dart
│   │       ├── chat_provider.dart
│   │       └── health_provider.dart
│   │
│   └── services/                    # 服务层
│       ├── api_service.dart         # API 服务
│       ├── auth_service.dart        # 认证服务
│       ├── health_service.dart      # 健康数据服务
│       ├── notification_service.dart # 通知服务
│       └── storage_service.dart     # 存储服务
│
├── assets/                          # 资源文件
│   ├── images/
│   ├── icons/
│   └── fonts/
│
├── test/                            # 测试
│   ├── unit/
│   ├── widget/
│   └── integration/
│
└── pubspec.yaml                     # 依赖配置
```

---

### 2.2 核心依赖

**pubspec.yaml**

```yaml
dependencies:
  flutter:
    sdk: flutter

  # 状态管理
  provider: ^6.1.1                    # 简单易用
  # 或 riverpod: ^2.4.9               # 更强大（推荐生产环境）

  # 网络请求
  dio: ^5.4.0                         # 强大的 HTTP 客户端
  retrofit: ^4.1.0                    # 类型安全的 API 客户端生成器

  # 本地存储
  shared_preferences: ^2.2.2          # 轻量级键值存储
  hive: ^2.2.3                        # 高性能 NoSQL 数据库
  hive_flutter: ^1.1.0

  # 健康数据
  health: ^8.1.0                      # HealthKit / Health Connect

  # UI 组件
  flutter_chat_ui: ^1.6.9             # 聊天界面
  fl_chart: ^0.66.0                   # 图表库
  cached_network_image: ^3.3.1        # 图片缓存

  # 工具库
  uuid: ^4.2.1                        # UUID 生成
  intl: ^0.19.0                       # 国际化
  url_launcher: ^6.2.3                # 打开链接
  package_info: ^2.0.2                # 应用信息

  # 环境变量
  flutter_dotenv: ^5.1.0              # 加载 .env 文件

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1               # Lint 规则
  build_runner: ^2.4.8                # 代码生成
  retrofit_generator: ^8.1.0          # Retrofit 生成器
  hive_generator: ^2.0.1              # Hive 生成器
```

---

### 2.3 状态管理方案

**使用 Provider（推荐入门）**

```dart
// lib/presentation/providers/chat_provider.dart

import 'package:flutter/foundation.dart';
import '../../data/models/message.dart';
import '../../services/api_service.dart';

class ChatProvider with ChangeNotifier {
  final ApiService _apiService;

  List<Message> _messages = [];
  bool _isLoading = false;
  String? _error;

  List<Message> get messages => _messages;
  bool get isLoading => _isLoading;
  String? get error => _error;

  ChatProvider(this._apiService);

  /// 发送消息
  Future<void> sendMessage(String content) async {
    // 添加用户消息
    final userMessage = Message(
      id: DateTime.now().millisecondsSinceEpoch.toString(),
      content: content,
      role: MessageRole.user,
      createdAt: DateTime.now(),
    );

    _messages.add(userMessage);
    notifyListeners();

    // 显示加载状态
    _isLoading = true;
    notifyListeners();

    try {
      // 调用 API
      final response = await _apiService.sendMessage(
        userId: 'user_001',
        message: content,
        history: _buildHistory(),
      );

      // 添加智能体回复
      final agentMessage = Message(
        id: DateTime.now().millisecondsSinceEpoch.toString(),
        content: response['reply'],
        role: MessageRole.agent,
        createdAt: DateTime.now(),
      );

      _messages.add(agentMessage);
      _error = null;
    } catch (e) {
      _error = e.toString();
    } finally {
      _isLoading = false;
      notifyListeners();
    }
  }

  /// 清空消息
  void clearMessages() {
    _messages.clear();
    notifyListeners();
  }

  /// 构建历史消息
  List<Map<String, String>> _buildHistory() {
    return _messages
        .map((m) => {
              'role': m.role == MessageRole.user ? 'user' : 'assistant',
              'content': m.content,
            })
        .toList();
  }
}
```

---

## 三、后端架构 (FastAPI)

### 3.1 项目结构

```
health_agent_backend/
├── app/
│   ├── __init__.py
│   ├── main.py                      # 应用入口
│   ├── config.py                    # 配置管理
│   │
│   ├── api/                         # API 路由
│   │   ├── __init__.py
│   │   ├── deps.py                  # 依赖注入
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py              # 认证接口
│   │   │   ├── chat.py              # 对话接口
│   │   │   ├── health.py            # 健康数据接口
│   │   │   └── plan.py              # 训练计划接口
│   │   └── router.py                # 路由汇总
│   │
│   ├── core/                        # 核心模块
│   │   ├── __init__.py
│   │   ├── security.py              # 安全相关
│   │   ├── config.py                # 配置
│   │   └── exceptions.py            # 异常处理
│   │
│   ├── models/                      # 数据库模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── message.py
│   │   ├── health_data.py
│   │   └── plan.py
│   │
│   ├── schemas/                     # Pydantic 模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── message.py
│   │   ├── health.py
│   │   └── plan.py
│   │
│   ├── services/                    # 业务逻辑
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── chat_service.py
│   │   ├── health_service.py
│   │   └── plan_service.py
│   │
│   ├── ai/                          # AI 模块
│   │   ├── __init__.py
│   │   ├── llm_client.py            # 大模型客户端
│   │   ├── prompts/                 # 提示词模板
│   │   │   ├── __init__.py
│   │   │   ├── health_coach.py
│   │   │   └── plan_generator.py
│   │   └── router.py                # AI 路由（本地/云端）
│   │
│   └── db/                          # 数据库
│       ├── __init__.py
│       ├── base.py
│       ├── session.py
│       └── init_db.py
│
├── alembic/                         # 数据库迁移
│   ├── versions/
│   └── env.py
│
├── tests/                           # 测试
│   ├── __init__.py
│   ├── conftest.py
│   └── test_api/
│
├── requirements.txt                 # Python 依赖
├── .env                             # 环境变量
├── alembic.ini                      # Alembic 配置
└── Dockerfile                       # Docker 配置
```

---

### 3.2 核心依赖

**requirements.txt**

```txt
# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6

# 数据库
sqlalchemy==2.0.31
alembic==1.13.2
asyncpg==0.29.0
redis==5.0.8

# AI 模型
anthropic==0.34.2
openai==1.37.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# 工具库
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
httpx==0.27.0

# 监控
prometheus-client==0.20.0
```

---

### 3.3 API 接口设计

**app/api/v1/chat.py**

```python
from fastapi import APIRouter, Depends, HTTPException
from typing import List, Optional
from app.schemas.message import MessageCreate, MessageResponse
from app.services.chat_service import ChatService
from app.api.deps import get_current_user

router = APIRouter()

@router.post("/chat", response_model=MessageResponse)
async def send_message(
    request: MessageCreate,
    current_user = Depends(get_current_user),
    chat_service: ChatService = Depends()
):
    """
    发送消息给健康智能体
    
    - **user_id**: 用户ID
    - **message**: 用户消息内容
    - **history**: 对话历史（可选）
    """
    try:
        response = await chat_service.process_message(
            user_id=current_user.id,
            message=request.message,
            history=request.history
        )
        return response
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))

@router.get("/chat/history", response_model=List[MessageResponse])
async def get_chat_history(
    limit: int = 50,
    offset: int = 0,
    current_user = Depends(get_current_user),
    chat_service: ChatService = Depends()
):
    """获取对话历史"""
    messages = await chat_service.get_history(
        user_id=current_user.id,
        limit=limit,
        offset=offset
    )
    return messages
```

---

### 3.4 大模型集成

**app/ai/llm_client.py**

```python
from anthropic import AsyncAnthropic
from typing import List, Dict, Optional
import os

class LLMClient:
    """大模型客户端（支持多模型切换）"""
    
    def __init__(self):
        self.claude_client = AsyncAnthropic(
            api_key=os.getenv("ANTHROPIC_API_KEY")
        )
        # 可以添加其他模型客户端
        
    async def generate(
        self,
        messages: List[Dict[str, str]],
        system_prompt: str,
        model: str = "claude-sonnet-4-20250514",
        max_tokens: int = 2048,
        temperature: float = 0.7
    ) -> str:
        """
        生成回复
        
        Args:
            messages: 对话历史
            system_prompt: 系统提示词
            model: 模型名称
            max_tokens: 最大 token 数
            temperature: 温度参数
        """
        response = await self.claude_client.messages.create(
            model=model,
            max_tokens=max_tokens,
            system=system_prompt,
            messages=messages,
            temperature=temperature
        )
        
        return response.content[0].text
    
    async def generate_with_tools(
        self,
        messages: List[Dict[str, str]],
        system_prompt: str,
        tools: List[Dict],
        model: str = "claude-sonnet-4-20250514"
    ):
        """支持工具调用的生成"""
        # TODO: 实现工具调用逻辑
        pass
```

---

## 四、数据库设计

### 4.1 核心表结构

**用户表 (users)**

```sql
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    hashed_password VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    avatar_url VARCHAR(500),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    is_active BOOLEAN DEFAULT TRUE,
    is_premium BOOLEAN DEFAULT FALSE,
    premium_expires_at TIMESTAMP
);
```

**对话历史表 (messages)**

```sql
CREATE TABLE messages (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    role VARCHAR(20) NOT NULL, -- 'user' or 'agent'
    content TEXT NOT NULL,
    intent VARCHAR(50), -- 意图识别
    tokens_used INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_user_created (user_id, created_at DESC)
);
```

**健康数据表 (health_data)**

```sql
CREATE TABLE health_data (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    date DATE NOT NULL,
    
    -- 运动数据
    steps INTEGER,
    distance_km DECIMAL(10, 2),
    calories_burned INTEGER,
    active_minutes INTEGER,
    
    -- 生理数据
    heart_rate_avg INTEGER,
    heart_rate_resting INTEGER,
    sleep_hours DECIMAL(4, 2),
    sleep_quality INTEGER, -- 1-100
    
    -- 其他
    weight_kg DECIMAL(5, 2),
    water_ml INTEGER,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    UNIQUE(user_id, date),
    INDEX idx_user_date (user_id, date DESC)
);
```

**训练计划表 (training_plans)**

```sql
CREATE TABLE training_plans (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(200) NOT NULL,
    goal VARCHAR(50), -- 'weight_loss', 'muscle_gain', 'health', etc.
    duration_weeks INTEGER,
    
    plan_content JSONB, -- 存储计划详情
    
    start_date DATE,
    end_date DATE,
    is_active BOOLEAN DEFAULT TRUE,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 五、关键技术决策

### 5.1 状态管理：Provider vs Riverpod

| 特性 | Provider | Riverpod |
|------|----------|----------|
| 学习曲线 | ⭐⭐⭐⭐⭐ 简单 | ⭐⭐⭐ 中等 |
| 类型安全 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 性能 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 测试友好 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 适用场景 | 入门、小项目 | 生产级项目 |

**推荐**:
- 学习阶段：Provider（简单易上手）
- 生产环境：Riverpod（更强大、类型安全）

---

### 5.2 本地存储方案

| 方案 | 性能 | 适用场景 | 学习成本 |
|------|------|----------|----------|
| SharedPreferences | ⭐⭐ | 简单键值对 | 低 |
| Hive | ⭐⭐⭐⭐⭐ | 结构化数据 | 中 |
| SQLite | ⭐⭐⭐⭐ | 复杂关系型数据 | 高 |
| ObjectBox | ⭐⭐⭐⭐⭐ | 大量数据 | 中 |

**推荐**: Hive（高性能、易用、适合移动端）

---

### 5.3 网络请求方案

| 方案 | 特点 | 适用场景 |
|------|------|----------|
| http (官方) | 简单、功能基础 | 小项目 |
| dio | 功能强大、拦截器、缓存 | 生产环境 |
| retrofit | 类型安全、代码生成 | 大型项目 |

**推荐**: Dio + Retrofit（开发效率高、类型安全）

---

## 六、部署架构

### 6.1 开发环境

```
本地开发：
├── Flutter App (Windows 桌面/Chrome)
├── FastAPI (localhost:8000)
├── PostgreSQL (Docker)
└── Redis (Docker)
```

### 6.2 生产环境

```
云服务器部署：
┌─────────────────────────────────────┐
│  Nginx (反向代理)                   │
│  ├── Flutter Web (可选)             │
│  └── FastAPI (Uvicorn)              │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│  应用服务器                          │
│  ├── FastAPI (多进程)               │
│  ├── Celery (异步任务)              │
│  └── 监控 (Prometheus + Grafana)    │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│  数据层                              │
│  ├── PostgreSQL (主从)              │
│  ├── Redis (哨兵)                   │
│  └── 对象存储 (OSS)                 │
└─────────────────────────────────────┘
```

---

## 七、开发路线图

### Phase 1: MVP 核心功能（4 周）

| 周 | 前端任务 | 后端任务 |
|----|----------|----------|
| 1 | 项目搭建、基础 UI | FastAPI 脚手架、数据库 |
| 2 | 对话界面、消息列表 | 对话 API、Claude 集成 |
| 3 | 健康数据看板 | 健康数据 API、数据分析 |
| 4 | 用户认证、个人中心 | 认证系统、测试部署 |

### Phase 2: 功能完善（3 周）

| 周 | 任务 |
|----|------|
| 5 | 训练计划生成、多模态交互 |
| 6 | 本地模型集成、离线模式 |
| 7 | 通知推送、订阅付费 |

### Phase 3: 优化发布（2 周）

| 周 | 任务 |
|----|------|
| 8 | 性能优化、测试 |
| 9 | 上架 Google Play、运营准备 |

---

## 八、成本估算

### 8.1 开发成本

| 项目 | 首月 | 后续每月 |
|------|------|----------|
| 云服务器 | ¥200 | ¥200-500 |
| 域名 | ¥100 | - |
| Claude API | ¥500-2000 | ¥1000-5000 |
| Google Play 账户 | ¥180 | - |
| **总计** | **¥980-2480** | **¥1200-5500** |

### 8.2 成本优化策略

1. **本地模型优先** - 降低 60-70% API 成本
2. **Redis 缓存** - 减少重复计算
3. **CDN 加速** - 降低带宽成本
4. **付费墙** - 免费用户限制调用次数

---

## 九、下一步行动

### 立即可做：

1. **创建 Flutter 项目** - `flutter create health_agent_app`
2. **搭建 FastAPI 后端** - 参考 `main.py` 骨架
3. **设计数据库** - 使用 PostgreSQL + Alembic
4. **集成 Claude API** - 测试对话功能

---

**总结**: 这是一个完整的技术架构设计，涵盖了前端、后端、数据库、AI 集成等各个方面。建议按照 Phase 1 的计划，先实现 MVP 核心功能，验证产品价值后再逐步完善。
