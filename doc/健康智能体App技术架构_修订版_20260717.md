# 健康智能体 App 技术架构设计（修订版）

**时间**: 2026-07-17
**项目名称**: 健康智能体 (Health Agent)
**目标**: 打造一款整合健康数据、提供智能健康建议的移动应用
**修订说明**: 本文档基于 `健康智能体App技术架构_20260708.md` 修订，修复了原架构中 4 个 P0 级问题、10 个 P1 级问题、7 个 P2 级建议，并补充了 7 个缺失的关键架构章节。修订依据见 `健康智能体App技术架构_审查报告_20260717.md`。

---

## 目录

1. [整体架构设计](#一整体架构设计)
2. [安全架构](#二安全架构) *(新增)*
3. [合规设计](#三合规设计) *(新增)*
4. [前端架构 (Flutter)](#四前端架构-flutter)
5. [后端架构 (FastAPI)](#五后端架构-fastapi)
6. [流式对话设计](#六流式对话设计) *(新增)*
7. [数据库设计](#七数据库设计)
8. [限流策略](#八限流策略) *(新增)*
9. [内容审核与安全红线](#九内容审核与安全红线) *(新增)*
10. [日志与监控](#十日志与监控) *(新增)*
11. [关键技术决策](#十一关键技术决策)
12. [部署架构](#十二部署架构)
13. [CI/CD 流程](#十三cicd-流程) *(新增)*
14. [测试策略](#十四测试策略) *(新增)*
15. [开发路线图](#十五开发路线图)
16. [成本估算](#十六成本估算)
17. [下一步行动](#十七下一步行动)

---

## 一、整体架构设计

### 1.1 架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                        用户层                                  │
├──────────────────────────────────────────────────────────────┤
│  Flutter App (移动端)                                         │
│  ├── 对话界面 (SSE 流式渲染)                                  │
│  ├── 健康数据看板                                             │
│  ├── 训练计划管理                                             │
│  ├── 个人中心 (含隐私设置)                                    │
│  └── 安全存储 (flutter_secure_storage)                       │
└──────────────────────────────────────────────────────────────┘
                      ↕ HTTPS / SSE (Server-Sent Events)
┌──────────────────────────────────────────────────────────────┐
│                     API 网关层                                 │
├──────────────────────────────────────────────────────────────┤
│  Nginx (反向代理 + HTTPS + 限流)                              │
│  └── FastAPI 后端服务                                         │
│      ├── 认证中间件 (JWT 验证 + 黑名单检查)                   │
│      ├── 限流中间件 (Redis 滑动窗口)                          │
│      ├── 内容审核中间件 (输入过滤 + 输出检查)                  │
│      ├── 对话路由 (含 SSE 流式)                               │
│      ├── 健康数据管理 (加密/解密)                              │
│      └── 计划生成                                             │
└──────────────────────────────────────────────────────────────┘
                            ↕
┌──────────────────────────────────────────────────────────────┐
│                        数据层                                  │
├──────────────────────────────────────────────────────────────┤
│  PostgreSQL (主数据库)                                        │
│  ├── 用户数据 (含合规字段)                                    │
│  ├── 对话历史 (含成本追踪)                                    │
│  ├── 健康数据 (敏感字段加密存储)                               │
│  └── 训练计划                                                 │
│                                                              │
│  Redis (缓存层)                                              │
│  ├── 会话缓存 (对话上下文)                                    │
│  ├── 热点数据缓存                                             │
│  ├── 限流计数器                                               │
│  └── JWT 黑名单                                               │
└──────────────────────────────────────────────────────────────┘
                            ↕
┌──────────────────────────────────────────────────────────────┐
│                       AI 服务层                               │
├──────────────────────────────────────────────────────────────┤
│  大模型 API (多模型降级)                                      │
│  ├── Claude API (主力，流式输出)                              │
│  ├── DeepSeek API (降级备用)                                  │
│  └── 重试机制 (指数退避，最多 3 次)                           │
│                                                              │
│  向量数据库 (Phase 2)                                         │
│  └── RAG 检索增强 (健康知识库)                                │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 与原架构的关键差异

| 维度 | 原架构 | 修订架构 |
|------|--------|----------|
| 对话输出 | 同步等待完整响应 | SSE 流式逐 token 输出 |
| 模型降级 | 仅提概念，无实现 | Claude → DeepSeek 自动降级 + 重试 |
| 安全存储 | shared_preferences (明文) | flutter_secure_storage (Keychain/Keystore) |
| 限流 | 无 | Redis 滑动窗口，免费/付费差异化 |
| 异常处理 | 直接暴露 str(e) | 内部记日志 + 对外通用提示 |
| 健康数据 | 明文存储 | 敏感字段 AES-256 加密 + 软删除 |
| 合规字段 | 无 | consent_version / consent_at / deleted_at |
| 内容审核 | 无 | 输入过滤 + 输出安全检查 |
| 生产架构 | 主从+哨兵+Celery | 单机 Docker 起步，用户 >500 再扩展 |
| 开发周期 | 9 周 | 7-9 个月（与修订版可行性分析对齐） |

---

## 二、安全架构 *(新增)*

### 2.1 认证流程

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  客户端   │     │  FastAPI  │     │  Redis   │     │PostgreSQL│
│          │     │          │     │          │     │          │
│ 1.提交    │────▶│          │     │          │     │          │
│ 账号密码  │     │          │     │          │     │          │
│          │     │ 2.查询用户│─────────────────────────▶│          │
│          │     │ 3.验证密码│     │          │     │          │
│          │     │ (bcrypt)  │     │          │     │          │
│          │     │          │     │          │     │          │
│          │◀────│ 4.签发    │     │          │     │          │
│ Access   │     │ Access   │     │          │     │          │
│ Token    │     │ Token    │     │          │     │          │
│ (30min)  │     │ +Refresh │     │          │     │          │
│          │     │ Token    │     │          │     │          │
│          │     │ (7天)    │     │          │     │          │
│          │     │          │     │          │     │          │
│ 5.携带    │────▶│ 6.验证   │     │          │     │          │
│ Token    │     │ JWT签名  │     │          │     │          │
│ 请求API  │     │ 7.检查   │────▶│ 查询     │     │          │
│          │     │ 黑名单   │     │ 黑名单   │     │          │
│          │◀────│ 8.返回   │     │          │     │          │
│          │     │ 数据     │     │          │     │          │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

### 2.2 JWT 策略

```python
# app/core/security.py

from datetime import datetime, timedelta, timezone
from jose import jwt, JWTError
from passlib.context import CryptContext
import os
import secrets

# 密码哈希
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

# JWT 配置
JWT_SECRET = os.getenv("JWT_SECRET")  # 生产环境必须从环境变量注入
JWT_ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30
REFRESH_TOKEN_EXPIRE_DAYS = 7

def create_access_token(data: dict) -> str:
    """签发 Access Token（30 分钟有效）"""
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire, "type": "access"})
    return jwt.encode(to_encode, JWT_SECRET, algorithm=JWT_ALGORITHM)

def create_refresh_token(data: dict) -> str:
    """签发 Refresh Token（7 天有效）"""
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(days=REFRESH_TOKEN_EXPIRE_DAYS)
    to_encode.update({"exp": expire, "type": "refresh", "jti": secrets.token_hex(16)})
    return jwt.encode(to_encode, JWT_SECRET, algorithm=JWT_ALGORITHM)

def verify_token(token: str) -> dict:
    """验证 Token，返回 payload"""
    try:
        payload = jwt.decode(token, JWT_SECRET, algorithms=[JWT_ALGORITHM])
        return payload
    except JWTError:
        raise HTTPException(status_code=401, detail="无效的认证凭证")

def hash_password(password: str) -> str:
    return pwd_context.hash(password)

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)
```

### 2.3 Token 刷新与黑名单

```python
# app/api/v1/auth.py

@router.post("/auth/refresh")
async def refresh_token(
    refresh_token: str,
    redis: Redis = Depends(get_redis)
):
    """用 Refresh Token 换取新的 Access Token"""
    payload = verify_token(refresh_token)
    if payload.get("type") != "refresh":
        raise HTTPException(status_code=401, detail="无效的刷新凭证")

    # 检查是否在黑名单中（用户登出时将 refresh token 加入黑名单）
    jti = payload.get("jti")
    if await redis.exists(f"blacklist:{jti}"):
        raise HTTPException(status_code=401, detail="凭证已失效")

    new_access = create_access_token({"sub": payload["sub"]})
    return {"access_token": new_access, "token_type": "bearer"}

@router.post("/auth/logout")
async def logout(
    refresh_token: str,
    current_user = Depends(get_current_user),
    redis: Redis = Depends(get_redis)
):
    """登出 — 将 Refresh Token 加入黑名单"""
    payload = verify_token(refresh_token)
    jti = payload.get("jti")
    expire = payload.get("exp") - int(datetime.now(timezone.utc).timestamp())
    await redis.setex(f"blacklist:{jti}", expire, "1")
    return {"message": "已退出登录"}
```

### 2.4 密码安全策略

| 策略 | 规则 |
|------|------|
| 最小长度 | 8 位 |
| 复杂度 | 至少包含字母 + 数字 |
| 哈希算法 | bcrypt（cost factor = 12） |
| 登录失败锁定 | 5 次失败后锁定 15 分钟（Redis 计数） |
| 密码重置 | 通过邮箱发送一次性链接（有效期 30 分钟） |

---

## 三、合规设计 *(新增)*

### 3.1 PIPL 合规要点

本项目处理用户健康数据，属于《个人信息保护法》(PIPL) 定义的"敏感个人信息"，必须满足以下要求：

| 合规要求 | 实现方案 |
|----------|----------|
| 单独同意 | 健康数据采集前弹出独立授权弹窗，用户明确同意后方可采集 |
| 告知义务 | 隐私政策明确说明数据采集范围、用途、存储期限、第三方共享情况 |
| 最小必要 | 仅采集与功能直接相关的数据（步数、心率等），不采集位置等无关数据 |
| 加密存储 | 健康数据敏感字段使用 AES-256-GCM 加密后存储 |
| 数据删除 | 支持用户注销账号，软删除 30 天后物理删除 |
| 授权版本追踪 | 记录用户同意的隐私政策版本号和时间 |
| 数据可携带 | 支持用户导出个人数据（JSON 格式） |
| 第三方共享 | 仅与 LLM API 共享必要数据，签署数据处理协议 |

### 3.2 健康数据授权流程

```
用户首次打开健康数据看板
        │
        ▼
┌───────────────────┐
│ 显示授权说明页面   │
│ (数据范围/用途/    │
│  存储期限)         │
└───────┬───────────┘
        │
        ▼
┌───────────────────┐     拒绝
│ 用户选择是否同意   │────────▶ 不采集健康数据，看板显示空状态
└───────┬───────────┘
        │ 同意
        ▼
┌───────────────────┐
│ 记录授权信息       │
│ - consent_version  │
│ - consent_at       │
│ - privacy_policy_v │
└───────┬───────────┘
        │
        ▼
┌───────────────────┐
│ 调用 HealthKit /   │
│ Health Connect    │
│ 系统级授权弹窗     │
└───────┬───────────┘
        │
        ▼
┌───────────────────┐
│ 开始采集健康数据   │
│ (加密存储)         │
└───────────────────┘
```

### 3.3 数据加密方案

```python
# app/core/encryption.py

from cryptography.hazmat.primitives.ciphers.aead import AESGCM
import os
import json
import base64

class DataEncryptor:
    """AES-256-GCM 数据加密器"""

    def __init__(self):
        # 密钥从环境变量注入，32 字节 = 256 位
        key = os.getenv("DATA_ENCRYPTION_KEY")
        if not key:
            raise RuntimeError("DATA_ENCRYPTION_KEY 未配置")
        self.aesgcm = AESGCM(key.encode()[:32])

    def encrypt(self, data: dict) -> bytes:
        """加密字典数据，返回 nonce + ciphertext"""
        plaintext = json.dumps(data).encode()
        nonce = os.urandom(12)  # 96-bit nonce
        ciphertext = self.aesgcm.encrypt(nonce, plaintext, None)
        return nonce + ciphertext

    def decrypt(self, encrypted: bytes) -> dict:
        """解密数据"""
        nonce = encrypted[:12]
        ciphertext = encrypted[12:]
        plaintext = self.aesgcm.decrypt(nonce, ciphertext, None)
        return json.loads(plaintext.decode())
```

### 3.4 用户数据删除流程

```python
# app/services/compliance_service.py

class ComplianceService:
    """合规服务 — 处理数据删除、导出等合规需求"""

    async def request_account_deletion(self, user_id: str, db: AsyncSession):
        """
        用户请求注销账号
        - 标记为软删除（deleted_at）
        - 30 天后物理删除
        - 立即吊销所有 Token
        """
        user = await db.get(User, user_id)
        user.deleted_at = datetime.now(timezone.utc)
        user.is_active = False
        await db.commit()

        # 吊销所有 Token（将用户加入全局黑名单）
        await redis.setex(f"user_deactivated:{user_id}", 30 * 86400, "1")

    async def export_user_data(self, user_id: str, db: AsyncSession) -> dict:
        """
        导出用户全部数据（数据可携带权）
        """
        return {
            "user_profile": await self._export_profile(user_id, db),
            "chat_history": await self._export_messages(user_id, db),
            "health_data": await self._export_health_data(user_id, db),
            "training_plans": await self._export_plans(user_id, db),
            "exported_at": datetime.now(timezone.utc).isoformat(),
        }
```

### 3.5 医疗器械风险规避

> **关键原则**：本应用定位为"健康运动指导工具"，不提供医疗诊断或治疗建议。

| 禁止行为 | 允许行为 |
|----------|----------|
| 诊断疾病（"你可能患有XX"） | 分析运动数据趋势 |
| 推荐药物或治疗方案 | 建议运动类型和强度 |
| 解读医学检查报告 | 提供健康生活方式建议 |
| 替代医生做出医疗决策 | 提醒用户如有不适请就医 |

系统提示词中嵌入安全红线（详见第九章）。

---

## 四、前端架构 (Flutter)

### 4.1 项目结构

```
health_agent_app/
├── lib/
│   ├── main.dart                    # 应用入口（含全局错误捕获）
│   ├── app.dart                     # App 配置
│   │
│   ├── core/                        # 核心模块
│   │   ├── config/
│   │   │   ├── app_config.dart      # 应用配置
│   │   │   ├── api_config.dart      # API 配置
│   │   │   └── env.dart             # 环境变量
│   │   ├── constants/
│   │   │   ├── app_colors.dart      # 颜色常量
│   │   │   ├── app_strings.dart     # 字符串常量
│   │   │   └── app_assets.dart      # 资源路径
│   │   ├── theme/
│   │   │   └── app_theme.dart       # 主题配置
│   │   ├── router/
│   │   │   └── app_router.dart      # GoRouter 路由配置 *(新增)*
│   │   └── utils/
│   │       ├── date_utils.dart      # 日期工具
│   │       ├── validators.dart      # 表单验证
│   │       ├── logger.dart          # 日志工具
│   │       └── error_reporter.dart  # 错误上报 *(新增)*
│   │
│   ├── data/                        # 数据层
│   │   ├── models/
│   │   │   ├── user.dart            # 用户模型 (freezed)
│   │   │   ├── message.dart         # 消息模型 (freezed)
│   │   │   ├── health_data.dart     # 健康数据模型 (freezed)
│   │   │   └── plan.dart            # 训练计划模型 (freezed)
│   │   ├── repositories/
│   │   │   ├── user_repository.dart
│   │   │   ├── chat_repository.dart
│   │   │   └── health_repository.dart
│   │   └── datasources/
│   │       ├── remote/
│   │       │   ├── api_client.dart  # Dio HTTP 客户端
│   │       │   ├── sse_client.dart   # SSE 流式客户端 *(新增)*
│   │       │   └── api_endpoints.dart
│   │       └── local/
│   │           ├── secure_storage.dart  # 安全存储 *(新增，替代 shared_preferences)*
│   │           ├── local_cache.dart     # Hive 缓存
│   │           └── cache_manager.dart
│   │
│   ├── domain/                      # 业务逻辑层
│   │   ├── entities/
│   │   │   ├── user_entity.dart
│   │   │   └── health_entity.dart
│   │   └── usecases/
│   │       ├── chat_usecase.dart
│   │       ├── health_usecase.dart
│   │       └── plan_usecase.dart
│   │
│   ├── presentation/                # 展示层
│   │   ├── screens/
│   │   │   ├── auth/
│   │   │   │   ├── login_screen.dart       *(新增)*
│   │   │   │   └── register_screen.dart    *(新增)*
│   │   │   ├── home/
│   │   │   │   └── home_screen.dart
│   │   │   ├── chat/
│   │   │   │   ├── chat_screen.dart
│   │   │   │   └── widgets/
│   │   │   │       ├── message_bubble.dart
│   │   │   │       ├── chat_input.dart
│   │   │   │       └── streaming_indicator.dart  *(新增)*
│   │   │   ├── health/
│   │   │   │   ├── health_screen.dart
│   │   │   │   ├── consent_screen.dart            *(新增)*
│   │   │   │   └── widgets/
│   │   │   │       ├── health_card.dart
│   │   │   │       └── steps_chart.dart
│   │   │   ├── plan/
│   │   │   │   ├── plan_screen.dart
│   │   │   │   └── widgets/
│   │   │   │       └── plan_card.dart
│   │   │   └── profile/
│   │   │       ├── profile_screen.dart
│   │   │       ├── settings_screen.dart
│   │   │       ├── privacy_screen.dart            *(新增)*
│   │   │       └── data_export_screen.dart         *(新增)*
│   │   ├── widgets/                 # 通用组件
│   │   │   ├── common_button.dart
│   │   │   ├── common_card.dart
│   │   │   ├── loading_indicator.dart
│   │   │   └── error_widget.dart                   *(新增)*
│   │   └── providers/               # 状态管理 (Riverpod)
│   │       ├── auth_provider.dart
│   │       ├── chat_provider.dart
│   │       └── health_provider.dart
│   │
│   └── services/                    # 服务层
│       ├── api_service.dart         # API 服务
│       ├── auth_service.dart        # 认证服务
│       ├── health_service.dart      # 健康数据服务
│       ├── notification_service.dart # 通知服务
│       └── storage_service.dart     # 存储服务
│
├── assets/                          # 资源文件
│   ├── images/
│   ├── icons/
│   └── fonts/
│
├── test/                            # 测试
│   ├── unit/
│   ├── widget/
│   └── integration/
│
└── pubspec.yaml                     # 依赖配置
```

### 4.2 核心依赖

**pubspec.yaml**

```yaml
dependencies:
  flutter:
    sdk: flutter

  # 状态管理 — 直接使用 Riverpod（不再用 Provider） *(修订)*
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # 路由管理 *(新增)*
  go_router: ^14.2.0

  # 网络请求
  dio: ^5.4.0
  retrofit: ^4.1.0

  # 本地存储
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # 安全存储 — 敏感信息不用 shared_preferences *(修订)*
  flutter_secure_storage: ^9.2.2    # *(新增)*
  shared_preferences: ^2.2.2        # 仅用于非敏感配置

  # 健康数据
  health: ^8.1.0

  # UI 组件
  fl_chart: ^0.66.0
  cached_network_image: ^3.3.1
  dash_chat_2: ^0.2.7               # *(修订)* 替代 flutter_chat_ui

  # 模型代码生成 *(新增)*
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0

  # 工具库
  uuid: ^4.2.1
  intl: ^0.19.0
  url_launcher: ^6.2.3
  package_info_plus: ^8.0.0         # *(修订)* 替代已停止维护的 package_info
  connectivity_plus: ^6.0.0         # *(新增)* 网络状态检测

  # 环境变量
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  retrofit_generator: ^8.1.0
  hive_generator: ^2.0.1
  freezed: ^2.5.2                   # *(新增)*
  json_serializable: ^6.8.0         # *(新增)*
  riverpod_generator: ^2.4.0        # *(新增)*
```

### 4.3 路由管理 *(新增)*

```dart
// lib/core/router/app_router.dart

import 'package:go_router/go_router.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

final appRouterProvider = Provider<GoRouter>((ref) {
  final authState = ref.watch(authProvider);

  return GoRouter(
    initialLocation: '/',
    redirect: (context, state) {
      final isLoggedIn = authState.isLoggedIn;
      final isOnLogin = state.matchedLocation == '/login';

      if (!isLoggedIn && !isOnLogin) {
        return '/login';
      }
      if (isLoggedIn && isOnLogin) {
        return '/';
      }
      return null;
    },
    routes: [
      GoRoute(path: '/login', builder: (ctx, state) => LoginScreen()),
      GoRoute(path: '/register', builder: (ctx, state) => RegisterScreen()),
      GoRoute(path: '/', builder: (ctx, state) => HomeScreen()),
      GoRoute(path: '/chat', builder: (ctx, state) => ChatScreen()),
      GoRoute(path: '/health', builder: (ctx, state) => HealthScreen()),
      GoRoute(path: '/health/consent', builder: (ctx, state) => ConsentScreen()),
      GoRoute(path: '/plan', builder: (ctx, state) => PlanScreen()),
      GoRoute(path: '/profile', builder: (ctx, state) => ProfileScreen()),
      GoRoute(path: '/profile/privacy', builder: (ctx, state) => PrivacyScreen()),
      GoRoute(path: '/profile/data-export', builder: (ctx, state) => DataExportScreen()),
    ],
  );
});
```

### 4.4 全局错误处理 *(新增)*

```dart
// lib/main.dart

import 'dart:async';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'app.dart';
import 'core/utils/error_reporter.dart';

void main() {
  runZonedGuarded(() {
    FlutterError.onError = (details) {
      FlutterError.presentError(details);
      ErrorReporter.report(details.exception, details.stack);
    };
    runApp(ProviderScope(child: HealthAgentApp()));
  }, (error, stack) {
    ErrorReporter.report(error, stack);
  });
}
```

```dart
// lib/core/utils/error_reporter.dart

class ErrorReporter {
  static Future<void> report(Object error, StackTrace? stack) async {
    // 开发环境：打印到控制台
    if (kDebugMode) {
      print('=== ERROR ===');
      print(error);
      print(stack);
    }
    // 生产环境：上报到错误监控服务（如 Sentry）
    // await Sentry.captureException(error, stackTrace: stack);
  }
}
```

### 4.5 安全存储服务 *(新增)*

```dart
// lib/data/datasources/local/secure_storage.dart

import 'package:flutter_secure_storage/flutter_secure_storage.dart';

class SecureStorage {
  static final SecureStorage _instance = SecureStorage._();
  factory SecureStorage() => _instance;
  SecureStorage._();

  final _storage = const FlutterSecureStorage(
    aOptions: AndroidOptions(encryptedSharedPreferences: true),
    iOptions: IOSOptions(accessibility: KeychainAccessibility.first_unlock),
  );

  // 存储 JWT Token
  Future<void> setAccessToken(String token) =>
      _storage.write(key: 'access_token', value: token);

  Future<String?> getAccessToken() =>
      _storage.read(key: 'access_token');

  Future<void> setRefreshToken(String token) =>
      _storage.write(key: 'refresh_token', value: token);

  Future<String?> getRefreshToken() =>
      _storage.read(key: 'refresh_token');

  // 清除所有敏感数据（登出时调用）
  Future<void> clearAll() => _storage.deleteAll();
}
```

### 4.6 状态管理 — Riverpod ChatProvider *(修订)*

```dart
// lib/presentation/providers/chat_provider.dart

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:uuid/uuid.dart';
import '../../data/models/message.dart';
import '../../data/datasources/remote/sse_client.dart';

class ChatNotifier extends StateNotifier<ChatState> {
  final Ref _ref;
  final SseClient _sseClient;
  final _uuid = const Uuid();

  ChatNotifier(this._ref, this._sseClient) : super(ChatState.initial());

  /// 发送消息（流式）
  Future<void> sendMessage(String content) async {
    // 1. 添加用户消息（使用 UUID，不用时间戳） *(修订 P1-3)*
    final userMessage = Message(
      id: _uuid.v4(),
      content: content,
      role: MessageRole.user,
      createdAt: DateTime.now(),
    );

    state = state.copyWith(
      messages: [...state.messages, userMessage],
      isStreaming: true,
      streamingContent: '',
      error: null,
    );

    // 2. 创建占位的 Agent 消息
    final agentMessageId = _uuid.v4();
    state = state.copyWith(
      messages: [...state.messages, Message(
        id: agentMessageId,
        content: '',
        role: MessageRole.agent,
        createdAt: DateTime.now(),
        isStreaming: true,
      )],
    );

    // 3. 通过 SSE 流式接收回复
    try {
      String fullResponse = '';

      await for (final chunk in _sseClient.streamMessage(
        message: content,
        history: _buildHistory(),  // 已截断 *(修订 P1-2)*
      )) {
        fullResponse += chunk;
        // 更新最后一条消息的内容
        final messages = List<Message>.from(state.messages);
        final lastIdx = messages.length - 1;
        messages[lastIdx] = messages[lastIdx].copyWith(content: fullResponse);
        state = state.copyWith(messages: messages, streamingContent: fullResponse);
      }

      // 4. 流式完成
      final messages = List<Message>.from(state.messages);
      final lastIdx = messages.length - 1;
      messages[lastIdx] = messages[lastIdx].copyWith(isStreaming: false);
      state = state.copyWith(messages: messages, isStreaming: false);

    } catch (e) {
      // 5. 错误处理 — 移除占位消息，显示错误
      final messages = state.messages.where((m) => m.id != agentMessageId).toList();
      state = state.copyWith(
        messages: messages,
        isStreaming: false,
        error: '生成回复失败，请重试',
      );
    }
  }

  /// 构建历史消息 — 截断到最近 10 条 *(修订 P1-2)*
  List<Map<String, String>> _buildHistory() {
    const maxHistory = 10; // 约 5 轮对话
    final recent = state.messages.length > maxHistory
        ? state.messages.sublist(state.messages.length - maxHistory)
        : state.messages;

    return recent
        .where((m) => m.content.isNotEmpty) // 排除空内容（流式占位）
        .map((m) => {
              'role': m.role == MessageRole.user ? 'user' : 'assistant',
              'content': m.content,
            })
        .toList();
  }

  void clearMessages() {
    state = ChatState.initial();
  }
}

// State 定义
@freezed
class ChatState with _$ChatState {
  const factory ChatState({
    @Default([]) List<Message> messages,
    @Default(false) bool isStreaming,
    @Default('') String streamingContent,
    String? error,
  }) = _ChatState;

  factory ChatState.initial() => const ChatState();
}

// Provider
final chatProvider = StateNotifierProvider<ChatNotifier, ChatState>((ref) {
  return ChatNotifier(ref, ref.watch(sseClientProvider));
});
```

---

## 五、后端架构 (FastAPI)

### 5.1 项目结构

```
health_agent_backend/
├── app/
│   ├── __init__.py
│   ├── main.py                      # 应用入口（含全局异常处理）
│   │
│   ├── api/                         # API 路由
│   │   ├── __init__.py
│   │   ├── deps.py                  # 依赖注入（含工厂函数） *(修订)*
│   │   ├── middleware/              # 中间件 *(新增)*
│   │   │   ├── __init__.py
│   │   │   ├── rate_limit.py        # 限流中间件
│   │   │   └── request_logging.py   # 请求日志中间件
│   │   └── v1/
│   │       ├── __init__.py
│   │       ├── auth.py              # 认证接口
│   │       ├── chat.py              # 对话接口（含 SSE 流式）
│   │       ├── health.py            # 健康数据接口
│   │       ├── plan.py              # 训练计划接口
│   │       └── compliance.py        # 合规接口（数据导出/删除） *(新增)*
│   │
│   ├── core/                        # 核心模块
│   │   ├── __init__.py
│   │   ├── security.py              # JWT + 密码哈希 *(修订)*
│   │   ├── encryption.py            # 数据加密 *(新增)*
│   │   ├── config.py                # 配置
│   │   └── exceptions.py            # 全局异常处理 *(修订)*
│   │
│   ├── models/                      # 数据库模型 (SQLAlchemy)
│   │   ├── __init__.py
│   │   ├── user.py                  # *(修订：含合规字段)*
│   │   ├── message.py               # *(修订：含成本追踪)*
│   │   ├── health_data.py           # *(修订：含加密+合规)*
│   │   └── plan.py
│   │
│   ├── schemas/                     # Pydantic 模型
│   │   ├── __init__.py
│   │   ├── user.py
│   │   ├── message.py
│   │   ├── health.py
│   │   └── plan.py
│   │
│   ├── services/                    # 业务逻辑
│   │   ├── __init__.py
│   │   ├── auth_service.py
│   │   ├── chat_service.py          # *(修订：含流式+降级)*
│   │   ├── health_service.py        # *(修订：含加密)*
│   │   ├── plan_service.py
│   │   └── compliance_service.py    # *(新增)*
│   │
│   ├── ai/                          # AI 模块
│   │   ├── __init__.py
│   │   ├── llm_client.py            # *(修订：多模型+流式+重试)*
│   │   ├── content_moderator.py     # 内容审核 *(新增)*
│   │   └── prompts/
│   │       ├── __init__.py
│   │       ├── health_coach.py      # 含安全红线 *(修订)*
│   │       └── plan_generator.py
│   │
│   └── db/                          # 数据库
│       ├── __init__.py
│       ├── base.py
│       ├── session.py
│       └── init_db.py
│
├── alembic/                         # 数据库迁移
│   ├── versions/
│   └── env.py
│
├── tests/                           # 测试
│   ├── __init__.py
│   ├── conftest.py
│   ├── test_api/
│   ├── test_services/
│   └── test_ai/
│
├── docker-compose.yml               # *(新增)*
├── Dockerfile
├── requirements.txt
├── .env.example                     # *(新增)*
├── alembic.ini
└── pytest.ini                       # *(新增)*
```

### 5.2 核心依赖

**requirements.txt**

```txt
# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.6

# 数据库
sqlalchemy==2.0.31
alembic==1.13.2
asyncpg==0.29.0
psycopg[binary]==3.2.1            # *(新增)* Alembic 迁移用同步驱动
redis==5.0.8

# AI 模型
anthropic==0.34.2
openai==1.37.0                    # DeepSeek 兼容 OpenAI SDK

# 认证 *(修订)*
pyjwt==2.9.0                      # 替代 python-jose（维护更活跃）
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# 数据加密 *(新增)*
cryptography==43.0.0

# 工具库
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
httpx==0.27.0
loguru==0.7.2                     # *(新增)* 替代标准 logging

# 限流 *(新增)*
slowapi==0.1.9                    # 备用：也可用 Redis 手写

# 监控
prometheus-client==0.20.0

# 测试 *(新增)*
pytest==8.3.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0
httpx==0.27.0                     # 用于 API 测试
```

### 5.3 应用入口与全局配置 *(修订)*

```python
# app/main.py

from fastapi import FastAPI, Request
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse
from loguru import logger
import sys

# 配置日志
logger.remove()
logger.add(sys.stderr, level="DEBUG" if DEBUG else "INFO")
logger.add("logs/app_{time:YYYY-MM-DD}.log", rotation="1 day", retention="30 days")

app = FastAPI(
    title="Health Agent API",
    version="1.0.0",
    description="健康智能体后端 API",
    docs_url="/docs" if DEBUG else None,    # 生产环境关闭文档 *(修订)*
    redoc_url="/redoc" if DEBUG else None,
    openapi_url="/openapi.json" if DEBUG else None,
)

# CORS 配置 *(修订)*
app.add_middleware(
    CORSMiddleware,
    allow_origins=CORS_ORIGINS,  # 生产环境配置具体域名，不用 "*"
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 全局异常处理 — 不暴露内部错误 *(修订 P0-3)*
@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    logger.exception(f"Unhandled exception on {request.method} {request.url.path}")
    return JSONResponse(
        status_code=500,
        content={"detail": "服务暂时不可用，请稍后重试"}
    )

# 注册路由
from app.api.v1 import auth, chat, health, plan, compliance
app.include_router(auth.router, prefix="/api/v1")
app.include_router(chat.router, prefix="/api/v1")
app.include_router(health.router, prefix="/api/v1")
app.include_router(plan.router, prefix="/api/v1")
app.include_router(compliance.router, prefix="/api/v1")

@app.get("/health")
async def health_check():
    return {"status": "ok"}
```

### 5.4 依赖注入 *(修订 P0-2)*

```python
# app/api/deps.py

from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.ext.asyncio import AsyncSession
from redis.asyncio import Redis

from app.db.session import get_db
from app.core.security import verify_token
from app.core.config import settings
from app.models.user import User
from app.services.chat_service import ChatService
from app.services.health_service import HealthService

security = HTTPBearer()

async def get_redis() -> Redis:
    """获取 Redis 连接"""
    from app.db.session import redis_client
    return redis_client

async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: AsyncSession = Depends(get_db),
    redis: Redis = Depends(get_redis),
) -> User:
    """验证 JWT 并返回当前用户"""
    token = credentials.credentials
    payload = verify_token(token)

    if payload.get("type") != "access":
        raise HTTPException(status_code=401, detail="无效的凭证类型")

    user_id = payload.get("sub")

    # 检查是否在全局黑名单中（账号已注销）
    if await redis.exists(f"user_deactivated:{user_id}"):
        raise HTTPException(status_code=401, detail="账号已注销")

    user = await db.get(User, user_id)
    if not user or not user.is_active or user.deleted_at is not None:
        raise HTTPException(status_code=401, detail="用户不存在或已禁用")

    return user

# 工厂函数 — Depends 必须传入可调用对象 *(修订 P0-2)*
def get_chat_service(
    db: AsyncSession = Depends(get_db),
    redis: Redis = Depends(get_redis),
) -> ChatService:
    return ChatService(db, redis)

def get_health_service(
    db: AsyncSession = Depends(get_db),
) -> HealthService:
    return HealthService(db)
```

### 5.5 对话 API 接口 *(修订)*

```python
# app/api/v1/chat.py

from fastapi import APIRouter, Depends, HTTPException
from fastapi.responses import StreamingResponse
from typing import List
import json
import logging

from app.schemas.message import MessageCreate, MessageResponse
from app.services.chat_service import ChatService
from app.api.deps import get_current_user, get_chat_service
from app.models.user import User
from app.ai.content_moderator import ContentModerator

logger = logging.getLogger(__name__)
router = APIRouter()

@router.post("/chat/stream")
async def send_message_stream(
    request: MessageCreate,
    current_user: User = Depends(get_current_user),
    chat_service: ChatService = Depends(get_chat_service),  # *(修订 P0-2)*
):
    """
    流式对话 — SSE 逐 token 返回 *(修订 P0-4)*

    返回格式: text/event-stream
    每条事件: data: {"content": "..."}\n\n
    结束事件: data: {"done": true}\n\n
    错误事件: data: {"error": "..."}\n\n
    """
    # 内容审核 — 输入过滤 *(新增)*
    if not ContentModerator.is_input_safe(request.message):
        raise HTTPException(status_code=400, detail="输入内容包含敏感信息，请修改后重试")

    async def event_stream():
        try:
            async for chunk in chat_service.process_message_stream(
                user_id=current_user.id,
                message=request.message,
                history=request.history,
            ):
                yield f"data: {json.dumps({'content': chunk}, ensure_ascii=False)}\n\n"
            yield f"data: {json.dumps({'done': True})}\n\n"
        except Exception as e:
            logger.exception("Stream chat failed")
            yield f"data: {json.dumps({'error': '生成回复失败，请稍后重试'}, ensure_ascii=False)}\n\n"

    return StreamingResponse(
        event_stream(),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",  # Nginx 不缓冲
        },
    )

@router.get("/chat/history", response_model=List[MessageResponse])
async def get_chat_history(
    limit: int = 50,
    offset: int = 0,
    current_user: User = Depends(get_current_user),
    chat_service: ChatService = Depends(get_chat_service),
):
    """获取对话历史"""
    messages = await chat_service.get_history(
        user_id=current_user.id,
        limit=min(limit, 100),  # 上限保护
        offset=offset,
    )
    return messages
```

### 5.6 LLM 客户端 — 多模型降级 + 流式 *(修订)*

```python
# app/ai/llm_client.py

from anthropic import AsyncAnthropic
from openai import AsyncOpenAI
from typing import List, Dict, AsyncGenerator, Optional
import os
import logging
import asyncio

logger = logging.getLogger(__name__)

class LLMClient:
    """
    大模型客户端 — 支持多模型降级 + 流式输出 + 重试 *(修订 P0-4 + P1-4)*

    降级链: Claude (主力) → DeepSeek (备用)
    重试: 指数退避，最多 3 次
    """

    def __init__(self):
        self.providers = {}
        self.provider_order = []

        # Claude (主力)
        claude_key = os.getenv("ANTHROPIC_API_KEY")
        if claude_key:
            self.providers['claude'] = AsyncAnthropic(
                api_key=claude_key,
                timeout=30.0,
                max_retries=3,  # SDK 内置重试
            )
            self.provider_order.append('claude')

        # DeepSeek (降级) — 兼容 OpenAI SDK
        deepseek_key = os.getenv("DEEPSEEK_API_KEY")
        if deepseek_key:
            self.providers['deepseek'] = AsyncOpenAI(
                api_key=deepseek_key,
                base_url="https://api.deepseek.com",
                timeout=30.0,
                max_retries=3,
            )
            self.provider_order.append('deepseek')

        if not self.provider_order:
            raise RuntimeError("未配置任何 LLM API Key")

    async def generate_stream(
        self,
        messages: List[Dict[str, str]],
        system_prompt: str,
        max_tokens: int = 2048,
    ) -> AsyncGenerator[str, None]:
        """
        流式生成 — 按降级链尝试，逐 token 返回

        降级逻辑：
        1. 尝试 Claude 流式
        2. Claude 失败 → 尝试 DeepSeek 流式
        3. 全部失败 → 抛出异常（由上层处理）
        """
        last_error = None

        for provider in self.provider_order:
            try:
                logger.info(f"Using LLM provider: {provider}")
                if provider == 'claude':
                    async for chunk in self._stream_claude(
                        messages, system_prompt, max_tokens
                    ):
                        yield chunk
                    return  # 成功则不再降级

                elif provider == 'deepseek':
                    async for chunk in self._stream_deepseek(
                        messages, system_prompt, max_tokens
                    ):
                        yield chunk
                    return

            except Exception as e:
                logger.warning(f"Provider {provider} failed: {e}, trying fallback...")
                last_error = e
                continue

        # 所有 provider 都失败
        raise RuntimeError(f"All LLM providers failed. Last error: {last_error}")

    async def _stream_claude(
        self,
        messages: List[Dict[str, str]],
        system_prompt: str,
        max_tokens: int,
    ) -> AsyncGenerator[str, None]:
        """Claude 流式输出"""
        async with self.providers['claude'].messages.stream(
            model="claude-sonnet-4-20250514",
            max_tokens=max_tokens,
            system=system_prompt,
            messages=messages,
        ) as stream:
            async for text in stream.text_stream:
                yield text

    async def _stream_deepseek(
        self,
        messages: List[Dict[str, str]],
        system_prompt: str,
        max_tokens: int,
    ) -> AsyncGenerator[str, None]:
        """DeepSeek 流式输出（OpenAI 兼容接口）"""
        # DeepSeek 使用 OpenAI 格式，system 消息放在 messages 首条
        full_messages = [{"role": "system", "content": system_prompt}] + messages

        stream = await self.providers['deepseek'].chat.completions.create(
            model="deepseek-chat",
            max_tokens=max_tokens,
            messages=full_messages,
            stream=True,
        )

        async for chunk in stream:
            if chunk.choices[0].delta.content:
                yield chunk.choices[0].delta.content

    async def generate(
        self,
        messages: List[Dict[str, str]],
        system_prompt: str,
        max_tokens: int = 2048,
    ) -> str:
        """非流式生成（用于计划生成等非交互场景）"""
        result = []
        async for chunk in self.generate_stream(messages, system_prompt, max_tokens):
            result.append(chunk)
        return ''.join(result)
```

### 5.7 ChatService — 流式处理 + Token 追踪 *(修订)*

```python
# app/services/chat_service.py

from sqlalchemy.ext.asyncio import AsyncSession
from redis.asyncio import Redis
from typing import List, Dict, AsyncGenerator, Optional
import logging
import time

from app.ai.llm_client import LLMClient
from app.ai.prompts.health_coach import SYSTEM_PROMPT
from app.ai.content_moderator import ContentModerator
from app.models.message import Message
from app.core.config import settings

logger = logging.getLogger(__name__)

class ChatService:
    def __init__(self, db: AsyncSession, redis: Redis):
        self.db = db
        self.redis = redis
        self.llm = LLMClient()

    async def process_message_stream(
        self,
        user_id: str,
        message: str,
        history: Optional[List[Dict]] = None,
    ) -> AsyncGenerator[str, None]:
        """
        流式处理用户消息

        1. 构建 LLM 上下文（系统提示 + 历史 + 当前消息）
        2. 流式生成回复
        3. 内容安全检查
        4. 保存对话记录
        """
        start_time = time.time()
        full_response = []

        # 构建消息列表
        messages = self._build_messages(history or [], message)

        # 流式生成
        async for chunk in self.llm.generate_stream(
            messages=messages,
            system_prompt=SYSTEM_PROMPT,
            max_tokens=2048,
        ):
            full_response.append(chunk)
            yield chunk

        # 生成完成后异步保存
        response_text = ''.join(full_response)
        elapsed_ms = int((time.time() - start_time) * 1000)

        # 内容安全检查 — 输出过滤 *(新增)*
        if not ContentModerator.is_output_safe(response_text):
            logger.warning(f"Unsafe output detected for user {user_id}")
            # 记录但不阻断（已流式输出，无法撤回）

        # 保存对话记录（含成本追踪字段） *(修订 P1-9)*
        await self._save_message(
            user_id=user_id,
            role='user',
            content=message,
        )
        await self._save_message(
            user_id=user_id,
            role='agent',
            content=response_text,
            response_time_ms=elapsed_ms,
        )

    def _build_messages(
        self, history: List[Dict], current_message: str
    ) -> List[Dict[str, str]]:
        """构建 LLM 消息列表 — 历史截断 *(修订 P1-2)*"""
        # 后端也做截断，不信任客户端
        max_history = 10
        recent = history[-max_history:] if len(history) > max_history else history

        messages = []
        for h in recent:
            messages.append({
                'role': h.get('role', 'user'),
                'content': h.get('content', ''),
            })
        messages.append({'role': 'user', 'content': current_message})
        return messages

    async def _save_message(
        self,
        user_id: str,
        role: str,
        content: str,
        response_time_ms: Optional[int] = None,
    ):
        """保存消息到数据库"""
        msg = Message(
            user_id=user_id,
            role=role,
            content=content,
            response_time_ms=response_time_ms,
        )
        self.db.add(msg)
        await self.db.commit()
```

---

## 六、流式对话设计 *(新增)*

### 6.1 SSE 协议

对话接口使用 Server-Sent Events (SSE) 实现流式输出：

```
客户端 POST /api/v1/chat/stream
    ↓
服务端返回 Content-Type: text/event-stream
    ↓
逐条推送事件:
    data: {"content": "你"}\n\n
    data: {"content": "好"}\n\n
    data: {"content": "！"}\n\n
    data: {"content": "今天"}\n\n
    ...
    data: {"done": true}\n\n
```

### 6.2 前端 SSE 客户端

```dart
// lib/data/datasources/remote/sse_client.dart

import 'dart:async';
import 'dart:convert';
import 'package:dio/dio.dart';
import '../local/secure_storage.dart';

class SseClient {
  final Dio _dio = Dio();
  final SecureStorage _storage = SecureStorage();

  /// 流式发送消息，返回逐 chunk 的 Stream
  Stream<String> streamMessage({
    required String message,
    required List<Map<String, String>> history,
  }) async* {
    final token = await _storage.getAccessToken();

    final response = await _dio.post<ResponseBody>(
      '${ApiConfig.baseUrl}/api/v1/chat/stream',
      data: {
        'message': message,
        'history': history,
      },
      options: Options(
        headers: {
          'Authorization': 'Bearer $token',
          'Accept': 'text/event-stream',
        },
        responseType: ResponseType.stream,
      ),
    );

    // 解析 SSE 流
    final stream = response.data!.stream
        .transform(utf8.decoder)
        .transform(const LineSplitter());

    String buffer = '';
    await for (final line in stream) {
      if (line.startsWith('data: ')) {
        buffer = line.substring(6);
        try {
          final json = jsonDecode(buffer);
          if (json['done'] == true) return;
          if (json['error'] != null) {
            throw Exception(json['error']);
          }
          if (json['content'] != null) {
            yield json['content'] as String;
          }
        } catch (e) {
          // JSON 不完整，等下一行
          continue;
        }
      }
    }
  }
}
```

### 6.3 断线重连策略

| 场景 | 处理方式 |
|------|----------|
| 网络中断 | 前端检测到连接断开后，显示"连接中断"提示，提供"重试"按钮 |
| SSE 超时 | 30 秒无数据则判定超时，自动重连一次 |
| 服务端错误 | 收到 `{"error": "..."}` 事件后，显示错误提示，不自动重连 |
| Token 过期 | 收到 401 后，自动用 Refresh Token 换取新 Token 并重试 |

---

## 七、数据库设计

### 7.1 用户表 *(修订 P1-8)*

```sql
-- 用户表（含合规字段）
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    hashed_password VARCHAR(255) NOT NULL,
    full_name VARCHAR(100),
    avatar_url VARCHAR(500),

    -- 订阅状态
    is_premium BOOLEAN DEFAULT FALSE,
    premium_expires_at TIMESTAMP,

    -- 合规字段 *(新增 P1-8)*
    consent_version VARCHAR(20),          -- 同意的隐私政策版本
    consent_at TIMESTAMP,                 -- 同意时间
    deleted_at TIMESTAMP,                 -- 软删除时间（PIPL 要求）
    last_login_at TIMESTAMP,              -- 最后登录时间（安全审计）

    -- 状态
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引（PostgreSQL 用 CREATE INDEX，不写在 CREATE TABLE 内）*(修订 P0-1)*
CREATE INDEX idx_users_email ON users (email);
CREATE INDEX idx_users_deleted_at ON users (deleted_at) WHERE deleted_at IS NOT NULL;
```

### 7.2 对话历史表 *(修订 P0-1 + P1-9)*

```sql
-- 对话历史表（含成本追踪）
CREATE TABLE messages (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    role VARCHAR(20) NOT NULL,               -- 'user' or 'agent'
    content TEXT NOT NULL,
    intent VARCHAR(50),                      -- 意图识别

    -- 成本追踪字段 *(新增 P1-9)*
    model_used VARCHAR(50),                  -- 使用的模型
    input_tokens INTEGER,                    -- 输入 token 数
    output_tokens INTEGER,                   -- 输出 token 数
    response_time_ms INTEGER,                -- 响应耗时（毫秒）
    is_cached BOOLEAN DEFAULT FALSE,         -- 是否命中缓存

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 索引单独创建 *(修订 P0-1)*
CREATE INDEX idx_messages_user_created ON messages (user_id, created_at DESC);
CREATE INDEX idx_messages_created_at ON messages (created_at DESC);
```

### 7.3 健康数据表 *(修订 P0-1 + P1-7)*

```sql
-- 健康数据表（敏感字段加密存储 + 合规字段）
CREATE TABLE health_data (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    date DATE NOT NULL,

    -- 健康数据（加密 JSON 存储 — 整体加密，性能优于逐字段加密）
    data_encrypted BYTEA,                   -- AES-256-GCM 加密的健康数据 JSON

    -- 元数据（不加密，用于查询过滤）
    data_source VARCHAR(20) NOT NULL DEFAULT 'health_kit',  -- 数据来源 *(新增 P1-7)*
    consent_version VARCHAR(20),            -- 授权时的隐私政策版本 *(新增 P1-7)*

    -- 合规字段
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP,                   -- 软删除 *(新增 P1-7)*

    UNIQUE(user_id, date)
);

-- 索引单独创建 *(修订 P0-1)*
CREATE INDEX idx_health_user_date ON health_data (user_id, date DESC);
CREATE INDEX idx_health_deleted_at ON health_data (deleted_at) WHERE deleted_at IS NOT NULL;
```

> **加密策略说明**：实际实现中将每日健康数据打包为 JSON，使用 AES-256-GCM 整体加密后存入 `data_encrypted` 字段。查询时先按 `user_id + date` 过滤（不需要解密），再对结果集解密。这比逐字段加密性能更好，且满足 PIPL 加密存储要求。

### 7.4 训练计划表

```sql
-- 训练计划表
CREATE TABLE training_plans (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    title VARCHAR(200) NOT NULL,
    goal VARCHAR(50),                        -- 'weight_loss', 'muscle_gain', 'health'
    duration_weeks INTEGER,

    plan_content JSONB,                      -- 计划详情

    start_date DATE,
    end_date DATE,
    is_active BOOLEAN DEFAULT TRUE,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_plans_user_active ON training_plans (user_id, is_active);
```

### 7.5 完整建表脚本

```sql
-- init.sql — 完整初始化脚本

-- 启用 UUID 扩展
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- 创建表（按依赖顺序）
CREATE TABLE users (...);
CREATE TABLE messages (...);
CREATE TABLE health_data (...);
CREATE TABLE training_plans (...);

-- 创建索引
CREATE INDEX idx_users_email ON users (email);
CREATE INDEX idx_users_deleted_at ON users (deleted_at) WHERE deleted_at IS NOT NULL;
CREATE INDEX idx_messages_user_created ON messages (user_id, created_at DESC);
CREATE INDEX idx_messages_created_at ON messages (created_at DESC);
CREATE INDEX idx_health_user_date ON health_data (user_id, date DESC);
CREATE INDEX idx_health_deleted_at ON health_data (deleted_at) WHERE deleted_at IS NOT NULL;
CREATE INDEX idx_plans_user_active ON training_plans (user_id, is_active);
```

---

## 八、限流策略 *(新增)*

### 8.1 限流规则

| 接口 | 免费用户 | 付费用户 | 窗口 |
|------|----------|----------|------|
| POST /chat/stream | 10 次/日 | 无限 | 24 小时 |
| GET /chat/history | 30 次/小时 | 无限 | 1 小时 |
| POST /health/data | 20 次/小时 | 无限 | 1 小时 |
| POST /plan/generate | 3 次/日 | 10 次/日 | 24 小时 |

### 8.2 Redis 滑动窗口实现

```python
# app/api/middleware/rate_limit.py

from redis.asyncio import Redis
from fastapi import HTTPException, Request
from datetime import date
import logging

logger = logging.getLogger(__name__)

RATE_LIMITS = {
    "chat": {"free": 10, "window": 86400},       # 10 次/日
    "history": {"free": 30, "window": 3600},      # 30 次/时
    "health_data": {"free": 20, "window": 3600},  # 20 次/时
    "plan": {"free": 3, "window": 86400},         # 3 次/日
}

async def check_rate_limit(
    user_id: str,
    is_premium: bool,
    resource: str,
    redis: Redis,
):
    """
    检查限流 — Redis 滑动窗口

    Args:
        user_id: 用户 ID
        is_premium: 是否付费用户
        resource: 资源类型（chat/history/health_data/plan）
        redis: Redis 连接
    """
    if is_premium:
        return  # 付费用户不限流

    config = RATE_LIMITS.get(resource)
    if not config:
        return  # 无配置则不限流

    key = f"rate_limit:{resource}:{user_id}:{date.today()}"
    count = await redis.incr(key)

    if count == 1:
        await redis.expire(key, config["window"])

    if count > config["free"]:
        remaining = 0
        logger.info(f"Rate limit exceeded for user {user_id}: {resource} ({count}/{config['free']})")
        raise HTTPException(
            status_code=429,
            detail={
                "message": f"今日{resource}次数已用完（{config['free']}次/日）",
                "upgrade_url": "/profile/subscription",
            }
        )
```

### 8.3 在路由中使用

```python
@router.post("/chat/stream")
async def send_message_stream(
    request: MessageCreate,
    current_user: User = Depends(get_current_user),
    chat_service: ChatService = Depends(get_chat_service),
    redis: Redis = Depends(get_redis),
):
    # 限流检查
    await check_rate_limit(
        user_id=str(current_user.id),
        is_premium=current_user.is_premium,
        resource="chat",
        redis=redis,
    )
    # ... 后续逻辑
```

---

## 九、内容审核与安全红线 *(新增)*

### 9.1 系统提示词安全红线

```python
# app/ai/prompts/health_coach.py

SYSTEM_PROMPT = """你是一个专业的健康运动指导助手。你的职责是：

## 你可以做的
- 分析用户的运动数据（步数、心率、睡眠等）并提供趋势解读
- 根据用户目标推荐运动类型、强度和频率
- 提供健康生活方式建议（饮食、睡眠、压力管理）
- 生成个性化的训练计划
- 回答关于运动健身的一般性问题

## 你绝对不能做的（安全红线）
- 诊断任何疾病或健康状况（如"你可能患有XX"）
- 推荐任何药物、保健品或治疗方案
- 解读医学检查报告（如血液检查、影像报告）
- 替代医生做出医疗决策
- 提供心理疾病诊断或治疗建议

## 当用户询问医疗问题时
你必须回复以下免责声明：
"我是一名健康运动指导助手，无法提供医疗诊断或治疗建议。如果您有健康方面的担忧，请咨询专业医生。我可以帮您分析运动数据、制定训练计划或提供健康生活方式建议。"

## 语气要求
- 专业但亲切
- 鼓励性，不打击用户积极性
- 用通俗易懂的语言，避免过多专业术语
- 回答简洁，不超过 300 字
"""
```

### 9.2 内容审核器

```python
# app/ai/content_moderator.py

import re
import logging

logger = logging.getLogger(__name__)

class ContentModerator:
    """内容审核 — 输入过滤 + 输出安全检查"""

    # 输入敏感词模式
    INPUT_BLOCKED_PATTERNS = [
        r'诊断.*病',
        r'开.*药',
        r'处方',
        r'治疗方案',
        r'吃药',
        r'用药',
    ]

    # 输出禁止模式
    OUTPUT_BLOCKED_PATTERNS = [
        r'你可能患有.{0,10}病',
        r'建议你服用.{0,10}药',
        r'诊断为.{0,10}病',
        r'你需要.{0,10}治疗',
    ]

    @classmethod
    def is_input_safe(cls, text: str) -> bool:
        """检查用户输入是否安全"""
        for pattern in cls.INPUT_BLOCKED_PATTERNS:
            if re.search(pattern, text):
                logger.warning(f"Blocked input: matched pattern '{pattern}'")
                return False
        return True

    @classmethod
    def is_output_safe(cls, text: str) -> bool:
        """检查 AI 输出是否安全"""
        for pattern in cls.OUTPUT_BLOCKED_PATTERNS:
            if re.search(pattern, text):
                logger.warning(f"Unsafe output detected: matched pattern '{pattern}'")
                return False
        return True
```

---

## 十、日志与监控 *(新增)*

### 10.1 日志配置

```python
# app/core/config.py (日志部分)

from loguru import logger
import sys

def setup_logging(debug: bool = False):
    logger.remove()

    # 控制台输出
    logger.add(
        sys.stderr,
        level="DEBUG" if debug else "INFO",
        format="<green>{time:HH:mm:ss}</green> | <level>{level:<8}</level> | <cyan>{name}</cyan> - {message}",
    )

    # 文件输出 — 按天轮转，保留 30 天
    logger.add(
        "logs/app_{time:YYYY-MM-DD}.log",
        level="INFO",
        rotation="1 day",
        retention="30 days",
        compression="zip",
        format="{time:YYYY-MM-DD HH:mm:ss} | {level:<8} | {name}:{function}:{line} - {message}",
    )

    # 错误日志单独文件
    logger.add(
        "logs/error_{time:YYYY-MM-DD}.log",
        level="ERROR",
        rotation="1 day",
        retention="90 days",
        compression="zip",
    )
```

### 10.2 请求日志中间件

```python
# app/api/middleware/request_logging.py

from fastapi import Request
from loguru import logger
import time

@app.middleware("http")
async def request_logging_middleware(request: Request, call_next):
    """记录每个请求的耗时和状态"""
    start = time.time()

    # 请求信息
    logger.info(f"→ {request.method} {request.url.path}")

    response = await call_next(request)

    # 响应信息
    elapsed_ms = int((time.time() - start) * 1000)
    logger.info(
        f"← {request.method} {request.url.path} "
        f"[{response.status_code}] {elapsed_ms}ms"
    )

    # 慢请求告警
    if elapsed_ms > 3000:
        logger.warning(f"Slow request: {request.url.path} took {elapsed_ms}ms")

    return response
```

### 10.3 监控指标

| 指标 | 说明 | 告警阈值 |
|------|------|----------|
| API 响应时间 P95 | 95% 请求的响应时间 | > 5 秒 |
| API 错误率 | 5xx 错误占比 | > 5% |
| LLM 调用成功率 | Claude/DeepSeek 调用成功率 | < 95% |
| LLM 降级触发次数 | Claude 降级到 DeepSeek 的次数 | > 10 次/小时 |
| 日活用户对话次数 | 平均每用户每日对话次数 | — |
| API 成本 | 每日 LLM API 费用 | > 预算上限 80% |
| Redis 内存使用 | Redis 内存占用 | > 80% |
| 数据库连接池 | 活跃连接数 | > 80% 上限 |

---

## 十一、关键技术决策

### 11.1 状态管理：直接用 Riverpod *(修订 P2-7)*

| 特性 | Provider | Riverpod |
|------|----------|----------|
| 学习曲线 | 简单 | 中等 |
| 类型安全 | 一般 | 优秀 |
| 性能 | 好 | 优秀 |
| 测试友好 | 一般 | 优秀 |
| 适用场景 | 小项目 | 生产级项目 |

**决策**：直接使用 Riverpod，避免后期从 Provider 迁移的痛苦。学习曲线略陡但一次性投入比迁移划算。

### 11.2 本地存储方案

| 方案 | 用途 | 说明 |
|------|------|------|
| flutter_secure_storage | JWT Token、用户 ID | 加密存储，Android 用 EncryptedSharedPreferences，iOS 用 Keychain |
| shared_preferences | 非敏感配置（主题、语言） | 明文存储，仅用于非敏感数据 |
| Hive | 对话缓存、离线数据 | 高性能 NoSQL |

### 11.3 网络请求方案

| 方案 | 用途 |
|------|------|
| Dio + Retrofit | REST API 请求 |
| 自建 SSE 客户端 | 流式对话（Dio 的 stream 模式） |

### 11.4 LLM 多模型策略

```
用户发送消息
    │
    ▼
┌─────────────┐    成功
│ Claude API  │────────▶ 流式返回
│ (主力)      │
└──────┬──────┘
       │ 失败（超时/限流/错误）
       ▼
┌─────────────┐    成功
│ DeepSeek API│────────▶ 流式返回
│ (降级)      │
└──────┬──────┘
       │ 失败
       ▼
┌─────────────┐
│ 返回错误     │
│ "请稍后重试" │
└─────────────┘
```

---

## 十二、部署架构

### 12.1 开发环境

```yaml
# docker-compose.yml *(新增 P2-5)*

version: '3.8'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: health_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dev_password
    ports: ['5432:5432']
    volumes: ['pgdata:/var/lib/postgresql/data']
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports: ['6379:6379']
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru

  backend:
    build: ./backend
    ports: ['8000:8000']
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    env_file: .env
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  pgdata:
```

### 12.2 生产环境 — MVP 阶段 *(修订 P1-10)*

```
单台云服务器（2C4G，¥200-300/月）:
├── Docker
│   ├── FastAPI (Uvicorn, 2 workers)
│   ├── PostgreSQL 16 (单实例)
│   └── Redis 7 (单实例)
├── Nginx (反向代理 + HTTPS + SSE 支持)
├── 日志: loguru → 文件 + logrotate 轮转
└── 备份: pg_dump 每日自动备份到对象存储

用户量 > 500 后再考虑扩展:
├── 数据库读写分离（PostgreSQL 主从）
├── Celery 异步任务（计划生成、数据导出）
├── Prometheus + Grafana 监控
└── 对象存储 (OSS) — 用户头像、导出文件
```

**Nginx SSE 配置要点**：

```nginx
location /api/v1/chat/stream {
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;

    # SSE 关键配置 — 禁用缓冲
    proxy_buffering off;
    proxy_cache off;
    proxy_set_header Connection '';
    proxy_http_version 1.1;
    chunked_transfer_encoding on;

    # 超时设置（SSE 长连接）
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}
```

---

## 十三、CI/CD 流程 *(新增)*

### 13.1 GitHub Actions 配置

```yaml
# .github/workflows/ci.yml

name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test_password
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7
        ports: ['6379:6379']

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - run: pip install pytest pytest-asyncio pytest-cov httpx
      - run: pytest --cov=app --cov-report=xml
      - uses: codecov/codecov-action@v4

  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
      - run: flutter pub get
      - run: flutter test
      - run: flutter analyze

  deploy:
    needs: [backend-test, frontend-test]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /opt/health-agent
            git pull origin main
            docker-compose build
            docker-compose up -d
            docker-compose exec backend alembic upgrade head
```

---

## 十四、测试策略 *(新增 P2-8)*

### 14.1 后端测试

```python
# tests/conftest.py

import pytest
import pytest_asyncio
from httpx import AsyncClient
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession
from app.main import app
from app.db.session import get_db

@pytest_asyncio.fixture
async def client():
    """测试用 HTTP 客户端"""
    async with AsyncClient(app=app, base_url="http://test") as ac:
        yield ac

@pytest_asyncio.fixture
async def db_session():
    """测试用数据库会话（使用测试数据库）"""
    engine = create_async_engine("postgresql+asyncpg://test:test@localhost/test_db")
    async with AsyncSession(engine) as session:
        yield session
        await session.rollback()
```

```python
# tests/test_api/test_chat.py

import pytest

@pytest.mark.asyncio
async def test_chat_stream(client, auth_headers):
    """测试流式对话接口"""
    response = await client.post(
        "/api/v1/chat/stream",
        json={"message": "你好", "history": []},
        headers=auth_headers,
    )
    assert response.status_code == 200
    assert response.headers["content-type"] == "text/event-stream"

@pytest.mark.asyncio
async def test_rate_limit(client, auth_headers):
    """测试限流"""
    for i in range(10):
        response = await client.post(
            "/api/v1/chat/stream",
            json={"message": f"test {i}", "history": []},
            headers=auth_headers,
        )
        if i < 10:
            assert response.status_code == 200
        else:
            assert response.status_code == 429
```

### 14.2 测试目标

| 层级 | 范围 | 覆盖率目标 | 工具 |
|------|------|------------|------|
| 单元测试 | Service 层、工具函数 | > 70% | pytest |
| API 测试 | 所有 REST 端点 | > 80% | httpx + pytest-asyncio |
| Widget 测试 | 关键交互组件 | 核心组件 | flutter_test |
| 集成测试 | 核心流程（登录→对话→健康数据） | 关键路径 | integration_test |
| E2E 测试 | 可选，MVP 阶段手动测试 | — | — |

---

## 十五、开发路线图

> 与修订版可行性分析对齐，从原来的 9 周调整为 7-9 个月（兼职开发）。 *(修订 P1-11)*

### Phase 1: MVP 核心功能（3-4 个月）

| 月份 | 前端任务 | 后端任务 | 合规任务 |
|------|----------|----------|----------|
| 第 1 月 | Flutter 环境搭建、项目脚手架、GoRouter 路由、登录/注册页面 | FastAPI 脚手架、Docker Compose、数据库表创建、JWT 认证 | 隐私政策起草、合规字段设计 |
| 第 2 月 | 对话界面（SSE 流式渲染）、消息列表、Riverpod 状态管理 | 对话 API（SSE 流式）、Claude API 集成、多模型降级、限流 | — |
| 第 3 月 | 健康数据看板、授权弹窗、图表组件 | 健康数据 API、数据加密/解密、HealthKit/Health Connect 对接 | 健康数据授权流程、数据加密实现 |
| 第 4 月 | 个人中心、设置页面、测试修复 | 用户认证完善、API 测试、性能优化 | 合规自查、隐私政策定稿 |

**Phase 1 交付物**：可上架的 MVP — 用户注册登录、AI 对话（流式）、健康数据看板（已授权）。

### Phase 2: 功能完善（2-3 个月）

| 月份 | 任务 |
|------|------|
| 第 5 月 | 训练计划生成、计划管理 UI |
| 第 6 月 | 订阅付费系统（Google Play Billing）、付费墙 |
| 第 7 月 | 通知推送、数据导出、账号注销流程 |

**Phase 2 交付物**：完整功能版 — 含训练计划、订阅付费、通知推送、合规数据管理。

### Phase 3: 优化与扩展（2 个月）

| 月份 | 任务 |
|------|------|
| 第 8 月 | 性能优化、RAG 知识库搭建、缓存策略 |
| 第 9 月 | 上架 Google Play、ASO 优化、运营准备 |

**Phase 3 交付物**：生产就绪版 — 性能优化完成、知识库增强、上架发布。

> **注意**：端侧 LLM 集成不在 9 个月计划内。优先做 RAG 知识库增强（效果更好、成本更低），端侧推理待生态成熟后再考虑。

---

## 十六、成本估算

### 16.1 开发成本（MVP 阶段）

| 项目 | 首月 | 后续每月 | 说明 |
|------|------|----------|------|
| 云服务器（2C4G） | ¥200 | ¥200 | 单机 Docker |
| 域名 | ¥100 | — | 年费 |
| Claude API | ¥500-2000 | ¥1000-3000 | 按用量计费 |
| Google Play 账户 | ¥180 | — | 一次性（$25） |
| Apple 开发者账号 | ¥680 | — | 如需 iOS（$99/年） *(新增)* |
| 对象存储 | ¥50 | ¥50 | 备份、头像 |
| **总计** | **¥1110-2910** | **¥1250-3250** | — |

### 16.2 成本优化策略

1. **多模型降级** — 日常对话用 DeepSeek（成本约为 Claude 的 1/5），复杂问题用 Claude
2. **Redis 缓存** — 相同问题命中缓存，减少 API 调用
3. **RAG 知识库** — 常见问题用 RAG 检索 + 小模型生成，降低成本
4. **付费墙** — 免费用户限制 10 次/日，控制 API 成本
5. **Token 优化** — 系统提示词精简、历史消息截断、max_tokens 控制

---

## 十七、下一步行动

### 立即可做（第 1 周）

1. **创建项目仓库** — GitHub/GitLab，配置 CI/CD
2. **搭建开发环境** — Docker Compose（PostgreSQL + Redis + FastAPI）
3. **创建 Flutter 项目** — `flutter create health_agent_app`
4. **配置 Riverpod + GoRouter** — 搭建项目骨架
5. **起草隐私政策** — 建议请法律顾问审核

### 第 2-4 周

6. **实现认证系统** — 注册、登录、JWT、Token 刷新
7. **搭建 FastAPI 脚手架** — 中间件、全局异常处理、日志
8. **集成 Claude API** — 流式输出 + DeepSeek 降级
9. **实现对话界面** — SSE 流式渲染、消息列表
10. **创建数据库表** — Alembic 迁移脚本

---

## 附录：修订记录

| 编号 | 问题级别 | 修订内容 | 对应审查报告条目 |
|------|----------|----------|-----------------|
| 1 | P0 | SQL INDEX 移到 CREATE TABLE 外 | P0-1 |
| 2 | P0 | Depends() 传入工厂函数 | P0-2 |
| 3 | P0 | 异常处理改为内部记日志 + 对外通用提示 | P0-3 |
| 4 | P0 | 新增 SSE 流式输出 | P0-4 |
| 5 | P1 | ChatProvider 从 AuthProvider 获取 userId | P1-1 |
| 6 | P1 | 对话历史截断到最近 10 条 | P1-2 |
| 7 | P1 | 消息 ID 使用 UUID | P1-3 |
| 8 | P1 | LLM 客户端多模型降级 | P1-4 |
| 9 | P1 | 新增 Redis 限流 | P1-5 |
| 10 | P1 | 新增 flutter_secure_storage | P1-6 |
| 11 | P1 | 健康数据表加密 + 合规字段 | P1-7 |
| 12 | P1 | users 表合规字段 | P1-8 |
| 13 | P1 | messages 表成本追踪字段 | P1-9 |
| 14 | P1 | 生产架构简化为单机 Docker | P1-10 |
| 15 | P1 | 开发路线图调整为 7-9 个月 | P1-11 |
| 16 | P2 | Flutter 依赖更新 | P2-1 |
| 17 | P2 | 后端依赖更新 | P2-2 |
| 18 | P2 | 新增 GoRouter 路由管理 | P2-3 |
| 19 | P2 | 新增全局错误处理 | P2-4 |
| 20 | P2 | 新增 Docker Compose | P2-5 |
| 21 | P2 | API 文档配置（生产环境关闭） | P2-6 |
| 22 | P2 | 状态管理直接用 Riverpod | P2-7 |
| 23 | P2 | 新增测试策略 | P2-8 |
| 24 | 新增 | 安全架构章节 | 缺失章节 |
| 25 | 新增 | 合规设计章节 | 缺失章节 |
| 26 | 新增 | 流式对话设计章节 | 缺失章节 |
| 27 | 新增 | 限流策略章节 | 缺失章节 |
| 28 | 新增 | 内容审核与安全红线章节 | 缺失章节 |
| 29 | 新增 | 日志与监控章节 | 缺失章节 |
| 30 | 新增 | CI/CD 流程章节 | 缺失章节 |

---

**总结**：本修订版技术架构在原架构基础上修复了全部 28 个问题并补充了 7 个缺失章节。架构方向不变（Flutter + FastAPI + PostgreSQL + Claude API + Clean Architecture），但在安全、合规、流式对话、限流、内容审核等关键环节做了完整补充。建议按修订后的 7-9 个月路线图执行，Phase 1 先交付纯云端 MVP，验证产品价值后再逐步完善。
